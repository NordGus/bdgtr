<%# locals: (account:) %>
<div class="pb-10">
  <p class="text-lg mb-3">Are you sure you want to delete <span class="font-bold"><%= account.name %></span>?</p>
  <p>This action can't be reversed</p>
</div>
<div class="flex justify-between gap-2 mt-3">
  <%= link_to "Yes",
              finances_account_path(account),
              class: "block flex-grow rounded-lg text-center px-3 py-2 font-bold bg-red-600 hover:bg-red-700",
              data: { turbo_method: :delete, turbo_frame: :account, action: "click->modal#close" }
  %>
  <a
    class="block flex-grow px-3 py-2 cursor-pointer rounded-lg
     text-center text-neutral-950 hover:text-neutral-100
     bg-neutral-300 hover:bg-neutral-400"
    data-action="click->modal#close"
  >No</a>
</div>

