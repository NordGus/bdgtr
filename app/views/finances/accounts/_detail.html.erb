<%# locals: (account:) -%>
<%= form_with(model: account, url: account.url, method: account.http_method) do |form| %>
  <% if account.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(account.errors.count, "error") %> prohibited this account from being saved:</h2>

      <ul>
        <% account.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="flex">
    <%= form.label :name, class: "flex-1" do %>
      <%= form.text_field :name, class: "w-full py-2 px-3 bg-neutral-100 dark:bg-neutral-800 rounded-lg border-[1px] border-neutral-300 dark:border-neutral-700" %>
    <% end %>
  </div>

  <%= form.submit "Save", class: "hidden" %>
<% end %>
